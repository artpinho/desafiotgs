@page
@model Cadastro.Pages_Enderecos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de Endereços</h1>

<p>
    <a asp-page="Create" class="btn btn-success">Adicionar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Endereco[0].Logradouro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Endereco[0].Cliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Endereco) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Logradouro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.Email)
            </td>
            <td>                
                <a asp-page="./Details" class="btn btn-primary" asp-route-id="@item.Id">Detalhes</a> 
                <a asp-page="./Edit" class="btn btn-warning" asp-route-id="@item.Id">Alterar</a>
                <a asp-page="./Delete" class="btn btn-danger" asp-route-id="@item.Id">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
